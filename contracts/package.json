{
  "name": "agri-marketplace-contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "send:eth": "node scripts/send-eth.js",
    "deploy": "hardhat run scripts/deploy.js --network ganache",
    "deploy:ganache": "hardhat run scripts/deploy.js --network ganache",
    "transfer:owner": "hardhat run scripts/transfer-ownership.js --network ganache"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.2"
  }
}